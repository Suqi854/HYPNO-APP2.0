---
authority_schema: tavernweave/total-design/v1
project_id: hypno-app-gai
title: 催眠APP（改）
status: driver-approved-design
updated: 2026-08-19
next_gate: real-host-validation
---

# 催眠APP（改） · 总设计案

## 目标与非目标

目标：制作 `催眠APP（改）` SillyTavern 角色卡，以远程 HypnoOS 前端为壳，保留 v4.3 主要玩法模块，去掉成就与任务，新增 API、规则、剧情、图标、破解版。玩家可生成任意角色、地点和设定，并自行创建首楼。

非目标：

- 不继承 v4.3 固定角色、斋明学园、固定地图和固定剧情。
- 不照搬成就与任务。
- 不把 API 密钥写入 JSON、PNG 或 GitHub。
- 不让破解版影响正文剧情。
- 不照搬会污染剧情的旧作弊模式。

## 项目类型与承载面

- 类型：Hybrid。
- 推荐承载：`hybrid`。
- 适配等级：`conditional`。
- 交付方式：GitHub/jsdelivr 远程加载。
- 降级路径：远端加载失败时显示错误状态，首版不做完整离线版。

## 用户价值与核心循环

1. 导入 JSON 或 PNG 卡。
2. 卡内启动器通过 jsdelivr 加载 GitHub 上的 HypnoOS 前端。
3. 配置自定义 API 或酒馆连接预设。
4. 生成角色草稿，在 APP 中预览，玩家确认后写入世界书、档案和 MVU。
5. 按单行文本添加地点并写入世界书。
6. 可选配置剧情大纲；也可保持无剧情大纲。
7. 设置永久/临时规则，选择悬浮图标，可选破解版。
8. 玩家自己创建首楼，正文模型只负责故事，不负责生成角色或设定。

## 四态决策账本

### 已确认

- 新卡名称为 `催眠APP（改）`。
- v4.3 仅作重点参考，不继承固定世界观。
- v4.3 除成就与任务外，其他主要玩法模块保留并通用化。
- 首版采用 GitHub/jsdelivr 远程加载。
- 新增 API、规则、剧情、图标、破解版模块。
- 中国日历使用新历公历，包含节气、中国法定节假日和常见西方节日。
- 角色生成采用“草稿预览、玩家确认、写入世界书、同步档案和 MVU”两阶段流程。
- 删除角色时同步删除世界书、关联条目、APP 档案和 MVU 对应节点。
- GitHub 目标仓库为 `Suqi854/HYPNO-APP2.0`，当前为空仓库，已授权方案通过后初始化 Git 和推送。

### 提案中

- 各 v4.3 模块的具体通用化字段和边界。
- 世界书条目命名、角色 ID 和删除回滚协议。
- 脚本、正则和前端模块之间的加载顺序。

### 待决定

- 无阻塞性待决定项；具体 API 协议和 MVU 结构在领域蓝图中细化。

### 已否决

- 原样保留 v4.3 全部脚本、正则和固定角色。
- 照搬成就与任务。
- 照搬固定学校、固定地图和固定剧情线。
- 照搬旧作弊模式。

## Core Spine

- 远程 HypnoOS 控制中心和启动器。
- 本地 API 配置与生成桥。
- 角色、地点、规则、剧情、图标、破解版模块。
- 世界书、APP 档案和 MVU 三处一致的角色同步。
- 空首楼，玩家自建开局。
- JSON/PNG 构建与 GitHub 远程发布链路。

## First Playable / First Usable

- 入口：玩家导入 JSON/PNG 卡到 SillyTavern。
- 主路径：远程前端加载、配置 API、生成并确认一个角色、添加一个地点、可选剧情大纲、设置一条规则、创建首楼。
- 状态源：世界书、MVU 变量、前端应用状态、玩家首楼。
- 必交付：源码项目、JSON 卡、PNG 卡、GitHub 仓库和构建产物。
- 不做：固定角色、固定开局、成就任务、密钥入库。
- 失败路径：远端加载失败、API 不可达、生成 JSON 无效、角色写入或删除半同步。
- 验收：自动化、静态预览、真实 SillyTavern、驾驶员确认分别负责，真实宿主不能由静态截图替代。
- 停止点：完成一个角色的生成确认和删除清理闭环，生成一段正文，确认角色/地点/规则进入上下文但不覆盖玩家开场。

## Growth Tracks

- 前端离线或本地回退包。
- 多 API 预设、模型模板、角色模板。
- GitHub/jsdelivr 自动更新流程。
- 更完整的日历数据维护和版本迁移。

## Parking Lot

- 旧 v4.3 固定世界和角色内容。
- 成就与任务，若未来重新设计可再评估。
- 云同步、账号和跨设备数据。
- 非首版必需的身体统计高级可视化。

## 稳定作品复用账本

| 来源 | 结论 | 新责任 |
|---|---|---|
| v4.3 | 重点参考/改造 | 复用模块机制，不继承固定世界观；去掉成就与任务。 |
| CM APP | 改造 | 复用通用角色、地点、规则写入桥。 |
| `1/HypnosisAPP5` | 改造 | 作为 HypnoOS 前端与构建工程底座。 |
| `2.0` | 仅参考 | 待确认与 1 的差异。 |

## 前端适配结论

- `recommended: hybrid`
- `fit: conditional`
- `prototypeGateRequired: true`
- `realHostGateRequired: true`

## 验收账本

- 自动化：schema、世界书、脚本、正则、构建产物、密钥脱敏检查。
- 静态预览：前端模块、远程加载、角色预览和错误状态。
- 真实宿主：SillyTavern 导入、远程前端、API 生成、世界书写入、角色删除清理、正文隔离。
- 驾驶员：首版闭环完成后逐项确认。

## 下一道门

First Playable 已进入实现候选阶段。下一道门是真实 SillyTavern 导入验证；真机通过后再处理 GitHub 初始化和推送。
